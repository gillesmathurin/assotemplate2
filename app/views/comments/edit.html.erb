<h1>Modifier le commentaire</h1>

<% form_for(@comment) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => {:f => f} %>
  
  <p>
    <%= f.submit 'Mettre à jour' %>
  </p>
<% end %>

<%= link_to 'Voir', @comment %> |
<%= link_to 'Retour', comments_path %>